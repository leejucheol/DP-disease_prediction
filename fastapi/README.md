### 실행 방법

**0. 필요한 모듈 설치**

```
pip install torch
pip install torch_geometric
```

**1. 현재 위치가 fastapi가 아니라면 fastapi 디렉토리로 이동**

```
cd fastapi
```

**2. raw 데이터 전처리 실행**

```
python src/preprocessor.py
```
